// project import
import dashboard from './dashboard';
import support from './support';
import services from './services';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, services, support]
};

export default menuItems;
